{% for locale in languages.locales %}
<a class="dropdown-item" href="{{ uri }}?locale={{ locale.language.getCode() }}">
    <img src="{{ asset('icons/flags/' ~ locale.getCode() ~ '.svg') }}">&nbsp;{{ locale.language.getName() }} ({{ locale.language.getLocal() }})</a>
{% endfor %}